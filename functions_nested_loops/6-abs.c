#include"main.h"
/**
 *_abs - function that calculates the absolute value of an integer
 *@n: integer to check
 *Return: n
 */
int _abs(int)
{
	if (n>= 0)
		return (n);

	else
	{
		n *= 1;
		return (n);
	}
}

