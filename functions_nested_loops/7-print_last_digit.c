#include"main.h"

/**
 * print_last_digit - prints last digit
 * @n: The number to be treated
 *
 * Return: 1
 */

int print_last_digit(int n)
{
	int 1,  p;


	1 = n % 10;
	if (1 < 0)
	1 = -1;
	p = '0' + 1;
	_putchar(p);
	return (1);
}
